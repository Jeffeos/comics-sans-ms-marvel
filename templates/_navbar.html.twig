<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/">CSMM</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor02">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item d-lg-inline-flex">
                <a class="nav-link {% if route_name == "app_index" %}active{% endif %}" href="/">Home</a>
                <a class="nav-link {% if route_name == "character_index" %}active{% endif %}" href="{{ path('character_index', { 'number': 20 }) }}">Characters</a>
            </li>
        </ul>
    </div>
</nav>